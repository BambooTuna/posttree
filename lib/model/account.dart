import 'package:flutter/cupertino.dart';

@immutable
class IdToken {
  final String id;
  IdToken({required this.id});
}

@immutable
class ServiceId {
  final String id;
  ServiceId({required this.id});
}

@immutable
class AccountId {
  final String id;
  AccountId({required this.id});
}
